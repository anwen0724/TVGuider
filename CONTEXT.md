# RTL 时序分析与修复

RTL 时序分析与修复的领域术语。

## Language

**案例（Case）**：一组对应的待修复 RTL、STA 时序报告及其约束和功能要求。比较修复效果时，双方必须使用同一组约束与功能要求。

**TVIR**：将 RTL 代码结构与对应 STA 报告中的时序路径和违例证据关联起来的联合表示，为根因分析和修复提供上下文。
_Avoid_: 将单独的 RTL 依赖图或原始时序报告称为完整 TVIR。

**修复知识库（Repair Knowledge Base）**：围绕时序问题组织的领域知识、修复策略和实例集合，供当前问题检索相关知识。

**基线（Baseline）**：某个案例在修改前的设计及其验证结果，是评价候选修复的参照。
_Avoid_: 把与当前 RTL、约束不匹配的历史报告称为基线。

**时序违例（Timing Violation）**：在指定约束和分析阶段下未满足的时序要求，包含相关路径、时钟和裕量等证据。

**结构路径（Structural Path）**：从 RTL 的信号与表达式依赖关系得到的路径，描述数据如何传播。
_Avoid_: 仅凭结构路径的节点数量把它称为时序关键路径。

**根因假设（Diagnosis）**：依据源码结构和违例证据提出的原因解释，需要通过修复及验证检验。
_Avoid_: 将模型解释或规则评分当作已经证明的根因。

**修复策略（Repair Strategy）**：针对特定根因、在明确适用条件下改变 RTL 的方法，包含预期效果和功能约束。

**候选修复（Candidate）**：为解决违例而提出的 RTL 版本，在验证完成之前不具有通过结论。

**重复采样（Sampling）**：对同一输入进行多次独立生成，不利用前一次候选的验证反馈更新输入。
_Avoid_: 反馈迭代。

**反馈迭代（Feedback Iteration）**：根据前一轮候选及验证结果调整下一轮的分析或修复输入。
_Avoid_: 用文件名中的 `iter` 或调用次数证明存在反馈闭环。

**验证结果（Validation Result）**：在明确的检查范围内获得的综合、功能和时序证据，各项检查可分别通过、失败、未执行或无法判定。
_Avoid_: 把综合成功、仿真通过或报告生成中的任意单项称为功能等价证明。

**时序改善（Timing Improvement）**：在可比较的分析条件下，候选相对于基线的目标时序指标得到改善。改善不一定意味着已消除全部违例。

**时序达标（Timing Target Met）**：指定分析阶段和路径范围内的时序指标满足既定目标。
_Avoid_: 将综合后达标表述为布局布线后的时序收敛。
